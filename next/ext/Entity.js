const
    _ = require('../util'),
    core = require('../core'),
    ext = require('.'),
    _private = new WeakMap();

class Entity extends core.Node {

    // TODO Entity

}

module.exports = Entity;