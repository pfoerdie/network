const
    _ = require('../util'),
    core = require('../core'),
    ext = require('.'),
    _private = new WeakMap();

class Relation extends core.Edge {

    // TODO Relation

}

module.exports = Relation;